import React from "react";
export default function Primeiro() {
  const msg = "Seja bem vindo(a)!";
  return (
    <div>
      <h2>
        Primeiro Componente
        <p> {msg}</p>
      </h2>
    </div>
  );
}
